---
title: Plan
description: Multi-step plan card with Shimmer-marked active step, status icons, and approve/regenerate footer.
---

import { PlanCollapsedPreview, PlanExpandedPreview, PlanStreamingPreview } from "@/components/preview/ai/chatbot";

## Preview

### Collapsed

<div className="rounded-lg border p-6">
  <PlanCollapsedPreview />
</div>

### Expanded

<div className="rounded-lg border p-6">
  <PlanExpandedPreview />
</div>

### Streaming

<div className="rounded-lg border p-6">
  <PlanStreamingPreview />
</div>

`Plan` is part of the **AI chatbot family** brought from Bridge into Gremorie in Fase 5j. It composes on top of other Gremorie primitives (`@gremorie/rx-display`, `@gremorie/rx-overlays`, `@gremorie/rx-forms`, ...) and ships with full Bridge parity.

## Install

<Tabs items={["Angular", "React"]}>

<Tab value="Angular">

Em breve. A categoria AI no Angular ainda so cobre PromptInput. Use a edicao React por enquanto, ou abra uma issue para priorizar este primitivo.

</Tab>

<Tab value="React">

```bash
npx gremorie add rx-plan
```

Pulls in the registry dependencies declared for `rx-plan` (cross-primitive Gremorie components - see `registry.json`).

</Tab>

</Tabs>

## Usage

<Tabs items={["Angular", "React"]}>

<Tab value="Angular">

Em breve.

</Tab>

<Tab value="React">

```tsx
import {
  Plan,
  PlanHeader,
  PlanTitle,
  PlanDescription,
  PlanAction,
  PlanContent,
  PlanStep,
  PlanFooter,
} from '@gremorie/rx-ai';

export function Demo() {
  return (
    <Plan isStreaming={isPlanning}>
      <PlanHeader>
        <PlanTitle>Generate report</PlanTitle>
        <PlanDescription>5 steps - 12 min</PlanDescription>
      </PlanHeader>
      <PlanContent>
        <PlanStep status="complete">Gather data</PlanStep>
        <PlanStep status="active">Build visualizations</PlanStep>
      </PlanContent>
    </Plan>
  );
}
```

</Tab>

</Tabs>

## Notes

- This primitive was migrated from Bridge into `@gremorie/rx-ai` as part of the Fase 5j chatbot batch (Bridge parity for the AI category).
- All cross-Gremorie imports use the published `@gremorie/rx-*` packages; the registry mirrors that into per-primitive source so consumers can `npx gremorie add rx-plan` without pulling unrelated AI components.
