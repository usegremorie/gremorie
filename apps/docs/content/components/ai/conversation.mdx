---
title: Conversation
description: Scroll-to-bottom container for chat messages. Stays anchored to the bottom while messages stream; ConversationScrollButton appears when the user scrolls up.
---

import { ConversationPreview, ConversationEmptyPreview } from "@/components/preview/ai/chatbot";

## Preview

### Standard

<div className="rounded-lg border p-6">
  <ConversationPreview />
</div>

### Empty state

<div className="rounded-lg border p-6">
  <ConversationEmptyPreview />
</div>

`Conversation` is part of the **AI chatbot family** brought from Bridge into Gremorie in Fase 5j. It composes on top of other Gremorie primitives (`@gremorie/rx-display`, `@gremorie/rx-overlays`, `@gremorie/rx-forms`, ...) and ships with full Bridge parity.

## Install

<Tabs items={["Angular", "React"]}>

<Tab value="Angular">

Em breve. A categoria AI no Angular ainda so cobre PromptInput. Use a edicao React por enquanto, ou abra uma issue para priorizar este primitivo.

</Tab>

<Tab value="React">

```bash
npx gremorie add rx-conversation
```

Pulls in the registry dependencies declared for `rx-conversation` (cross-primitive Gremorie components - see `registry.json`).

</Tab>

</Tabs>

## Usage

<Tabs items={["Angular", "React"]}>

<Tab value="Angular">

Em breve.

</Tab>

<Tab value="React">

```tsx
import {
  Conversation,
  ConversationContent,
  ConversationScrollButton,
  ConversationEmptyState,
} from '@gremorie/rx-ai';

export function Demo() {
  return (
    <Conversation>
      <ConversationContent>
        {messages.map((m) => <Message key={m.id} from={m.role}>...</Message>)}
      </ConversationContent>
      <ConversationScrollButton />
    </Conversation>
  );
}
```

</Tab>

</Tabs>

## Notes

- This primitive was migrated from Bridge into `@gremorie/rx-ai` as part of the Fase 5j chatbot batch (Bridge parity for the AI category).
- All cross-Gremorie imports use the published `@gremorie/rx-*` packages; the registry mirrors that into per-primitive source so consumers can `npx gremorie add rx-conversation` without pulling unrelated AI components.
