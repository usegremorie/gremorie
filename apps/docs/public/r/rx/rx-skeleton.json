{
  "$schema": "https://gremorie.com/schema/registry-item.json",
  "name": "rx-skeleton",
  "type": "registry:block",
  "framework": "rx",
  "title": "Skeleton (React)",
  "description": "Placeholder block for loading states - a pulsing rectangle that reserves layout space so there is no shift when data arrives. Honors `prefers-reduced-motion` automatically.",
  "categories": [
    "feedback"
  ],
  "dependencies": [],
  "devDependencies": [],
  "registryDependencies": [
    "rx-utils"
  ],
  "files": [
    {
      "path": "src/lib/skeleton/skeleton.tsx",
      "content": "import * as React from 'react';\n\nimport { cn } from '@gremorie/rx-core';\n\n/**\n * Skeleton - placeholder block for loading states.\n *\n * A pulsing rectangle that stands in\n * for content that is still being fetched. Shape it with className\n * width/height to match the geometry of the real content - this is\n * the whole point: skeletons reserve layout so there is no shift\n * when the data arrives. See [[ui/layout/cumulative-layout-shift]].\n *\n * The default animation is `animate-pulse`. Users with\n * `prefers-reduced-motion` see the static state - no extra config\n * needed. Use Skeleton as a presentation primitive only; pair with\n * `aria-busy=\"true\"` and `aria-live=\"polite\"` on the surrounding\n * region so screen readers announce the loading state.\n */\nfunction Skeleton({ className, ...props }: React.ComponentProps<'div'>) {\n  return (\n    <div\n      data-slot=\"skeleton\"\n      className={cn('animate-pulse rounded-md bg-accent', className)}\n      {...props}\n    />\n  );\n}\n\nexport { Skeleton };\n",
      "type": "registry:component",
      "target": "src/components/gremorie/feedback/skeleton/skeleton.tsx"
    }
  ],
  "cssVars": {},
  "usage": {
    "whenToUse": "TBD - document when to use rx-skeleton.",
    "whenNotToUse": "TBD - document when to reach for something else.",
    "bestPractices": [],
    "antipatterns": []
  }
}