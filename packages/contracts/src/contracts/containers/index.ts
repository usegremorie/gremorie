import { aspectRatio } from './aspect-ratio.contract';
import { resizable } from './resizable.contract';
import { scrollArea } from './scroll-area.contract';
import { stack } from './stack.contract';

/** All `containers` category contracts. */
export const CONTAINERS_CONTRACTS = [aspectRatio, resizable, scrollArea, stack];
