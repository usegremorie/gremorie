import { alert } from './alert.contract';
import { progress } from './progress.contract';
import { skeleton } from './skeleton.contract';
import { spinner } from './spinner.contract';

/** All `feedback` category contracts. */
export const FEEDBACK_CONTRACTS = [alert, progress, skeleton, spinner];
